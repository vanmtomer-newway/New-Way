# נתונים שיורדים מחדש — ~140MB, לא שייך ל-git
data/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# סודות — אין כאלה בפרויקט כרגע, אבל מוגדר מראש
.env
*.key
credentials*.json

# קבצי נעילה של Excel
~$*

# מונה בקשות RentCast — מצב מקומי של המפתח של תומר, משתנה בכל ריצה. הקלאוד בלי מפתח ממילא
rentcast_quota.json
