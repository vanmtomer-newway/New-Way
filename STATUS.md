# דרך חדשה — מערכת החיתום

**Last update:** 2026-09-06
**Changed:** **RentCast עובד** — תומר הפעיל את התוכנית החינמית; `listings.py --rentcast`
החזיר 913 מודעות פעילות ב-12 זיפי היעד (Redfin: 350 בייצוא). המכסה: 50 בקשות
בחודש, 12 לריצה ⇒ שבועי = 48; snapshot יומי ב-`data/rentcast/` מונע בזבוז על
ריצה חוזרת. **כלל ההצעה לפי שכבה יושם עד הסוף:** 72% ב-BUY BOX, 69% בצפון — קוד,
אקסל (B76 לפי תא הזיפ) ומסמכים. שורת התקדמות בשליפת שווי השומה (913 × ~0.45 שנ').
קודם לכן: **`listings.py` הורץ על ייצוא אמיתי של Redfin** — 27 העמודות תואמות,
102 מודעות ב-12 זיפי היעד ב-27 שניות. בלי ארגומנטים הוא אוסף `redfin_*.csv`
מ-Downloads; חותם רק את 12 זיפי היעד (זיפ חדש = גיאוקוד של דקות); שווי שומה
ב-4 תהליכונים. ממצא: בשכבה הצפונית כלל ה-72% לא כיסה את תרחיש התחתון (שגיאה
18.7%) — הוחלט על 69% שם. **פוש: ידני ע"י תומר** — למשתמש הקלוד
(`tomer-collab`) אין הרשאות, בכוונה. קומיט מקומי בלבד.
קודם לכן: יושמו ההחלטות מחלק ג' של הביקורת: **כלל ההצעה 72%** (היה 70%,
במקום אחד — `sdf.RULE` — ובאקסל); **שכבה צפונית 46220/46205/46260/46240** נוספה
ל-12 זיפי יעד אחרי בדיקה עמוקה (`docs/בדיקה_זיפים_צפוניים.md`: אותו בית
+3.5-5.3% בשנה, 148 פליפים עוברים ב-24 ח'); **שגיאת ARV לפי שכבה** (BUY BOX
12.4%/42.9%, צפון 18.7%/57.4%); **יומן חיתום** `underwriting_log.csv`; **מתחרים
מהנתונים** (`analyze.py rivals` פותח ב-15 הקונים הגדולים); **איחוד טעינה** —
שמות קונים על כל רשומה, 4 לולאות נמחקו; **6 תיקוני אקסל** בוצעו ב-openpyxl.
קודם לכן: נבנו ארבעת הכלים שאושרו מהביקורת: **`listings.py` — חיתום בכמות
של מודעות פעילות** מ-CSV של Redfin (או RentCast, לא נבדק) עם שווי שומה
מרשומות השומה; **מזהה הפליפים מסנן בנייה חדשה** (1,159 פליפים במקום 1,246,
מעבר 16% במקום 19%, מכפיל 1.555); **עדכניות** — כל הרצה מדפיסה "נתונים עד",
`sdf.py refresh` מוריד רק את השנה הנוכחית, השנה נקבעת מהלוח; **`arv.py` יושר
לאקסל** — שיפוץ לפי `--sqft --year`, עלויות רכישה $3,865 קבוע. 4 selftests
עוברים, המפה נבנתה מחדש, 3 רשומות ב-`decisions.md`.
קודם לכן: ביקורת מקצה לקצה — `docs/ביקורת_מודל_וכלים.md` + `docs/experiments/`.
קודם לכן: נוסף `analyze.py inventory` — **המלאי החי של המתחרים** מרשומות
השומה: 412 חלקות, 123 בזיפי ה-BUY BOX, עם זמן החזקה בפועל. גם כשכבה במפה.
הממצא: HomeGo מחזיקה 2 חלקות בלבד (עסק של מהירות), GRISE יושבת על 170.
קודם לכן: נוספו למפה שכבת מחוזות (תוויות + תקרת מס רכוש) ושכבת סניפי
Momentum Title — שהיא Hocker Title שנרכשה 22.4.2025, כלומר חברת הטייטל
שהנתונים שלנו הצביעו עליה. נוצר `docs/פניות_חוסמי_ההצעה.md`.
**חסם ההטבות לקונה נמדד** מ-`E3_Seller_Points` ואישר את עלות היציאה 8.25%.
קודם לכן: נוסף `arv.py` — מנוע ARV בקומפס גאוגרפיים, שמחליף את מכפיל
ה-1.18 בטווח מדוד. **חסם ה-$/sqft נסגר: נמדד ששטח בנוי לא יפתור את הבעיה**
(בקרה על גודל משפרת 13.6%→12.6% בלבד), ולכן Regrid/Shovels נפסלו.
הכלי פסל בהצלחה עסקה אמיתית שהפסידה $5,928.
**ובנוסף — תיקון מתחרים: `Power House Holdings` הוא HomeGo**, פלטפורמת מזומן
ארצית מטקסס, ולא מפעיל מקומי אלמוני. אותר דרך VPN אמריקאי + INBiz.

| | |
|---|---|
| **Status** | 🟢 הכל עובד על 12 זיפי היעד. ARV לפי שכבה: BUY BOX ±12.4%, צפון ±18.7% · כלל 72%/69% לפי שכבה · `listings.py` חותם CSV של Redfin **ו-RentCast (אומת, 913 מודעות)**. פוש ידני ע"י תומר |
| **Where** | `~/Projects/New-Way` |
| **Running** | ❌ כלום לא רץ ברקע. הכל ידני, לפי דרישה |
| **Depends on** | Python 3 (ספריית תקן בלבד). `RENTCAST_API_KEY` ב-`.env` (חינם, 50 בקשות/חודש) ל-`listings.py --rentcast`; בלעדיו — CSV של Redfin |

## פקודות

```bash
cd ~/Projects/New-Way

open deals_map.html        # 👈 המפה. נפתחת ישירות בדפדפן, בלי שרת

python3 map.py             # בונה מחדש את המפה
python3 sdf.py report      # תמונת מצב לכל זיפ ב-BUY BOX
python3 sdf.py flips       # הפליפים שנמצאו, לפי גודל הרווח
python3 analyze.py filter  # למה כלל ה-70% תופס 16%, ואיך מרחיבים
python3 sdf.py refresh     # מוריד מחדש רק את השנה הנוכחית (~25 שניות)
python3 analyze.py rivals  # מי עושה את העסק שלך (15 הגדולים, מהנתונים) + רשימת הייחוס

# 👇 חיתום נכס בודד
python3 arv.py comps "6259 Chadworth Court"                 # קומפס + טווח ARV
python3 arv.py deal "6259 Chadworth Court" --offer 241469   # טווח הרווח — ונרשם ב-underwriting_log.csv
python3 arv.py backtest    # מוכיח את הדיוק — ~18 שניות

# 👇 חיתום בכמות — מודעות פעילות מ-Redfin ("Download All" בתחתית החיפוש → Downloads)
python3 listings.py --rentcast                               # 👈 השבועי: 12 זיפי היעד מ-RentCast (12 בקשות מתוך 50 בחודש)
python3 listings.py                                          # או מ-CSV של Redfin ב-Downloads
python3 listings.py --dom 30                                 # רק מודעות שיושבות מעל 30 יום
python3 listings.py --comps "646 E 51ST ST"                  # הקומפס של מודעה אחת

python3 sdf.py selftest    # בדיקה עצמית — חייב להדפיס "selftest: ok"
python3 analyze.py selftest
python3 arv.py selftest
python3 listings.py selftest
```

**Check it's alive:** `python3 sdf.py selftest && python3 analyze.py selftest &&
python3 arv.py selftest && python3 listings.py selftest` — ארבעה `ok` = הלוגיקה תקינה.
**Start / Stop / Restart:** לא רלוונטי — אין תהליך רקע. המפה היא קובץ סטטי.
**נתונים ישנים?** כל הרצה מדפיסה "נתונים עד <תאריך>" ומזהירה מעל 14 יום.
`python3 sdf.py refresh` מוריד מחדש רק את השנה הנוכחית (~25 שניות; המדינה
מעדכנת כל שישי, מריון מגישה בפיגור ~חודשיים). `rm -rf data/` = הכל מחדש, ~4 דקות.

## מה יש כאן

- `deals_map.html` — **המפה.** 1.1MB, נפתחת בדפדפן. צריך אינטרנט לאריחי המפה
- `map.py` — בונה את המפה
- `sdf.py` — מנוע הנתונים: הורדה, פרסור, זיהוי פליפים, גיאוקודינג
- `analyze.py` — ניתוח המסנן והמתחרים
- `arv.py` — **חיתום נכס בודד.** קומפס גאוגרפיים + טווח רווח, לא מספר בודד
- `listings.py` — **חיתום בכמות.** CSV של Redfin (או RentCast) → כל המודעות
  ב-12 זיפי היעד ממוינות לפי הפער מהצעת ה-72%. `.env.example` — שם המפתח האופציונלי
- `underwriting_log.csv` — **יומן החיתום.** כל הרצת `arv.py deal`. עמודת `outcome` ידנית. לא למחוק
- `docs/בדיקה_זיפים_צפוניים.md` — **למה 46220/46205/46260/46240 נוספו**, ומה שונה שם בפרודקט
- `docs/מסנן_החיתום_ומתחרים.md` — **התשובות המלאות.** למה 16%, איך מרחיבים,
  המודל העסקי של כל מתחרה, ואזהרות הנתונים
- `מודל_חיתום_פליפ_אינדיאנפוליס.xlsx` — מודל החיתום, 6 גיליונות
- `CLAUDE.md` — קונטקסט העסק + הוראות לקוד
- `docs/מחקר_גאוגרפי_אינדיאנפוליס.md` — פרופיל 5 חלקים של המטרו
- `docs/פניות_חוסמי_ההצעה.md` — **שלוש הפניות לסגירת חוסמי ההצעה**, מוכנות
  להעתקה, כולל למי לפנות מאפס ומה לחפש בתשובה
- `docs/ביקורת_מודל_וכלים.md` — **ביקורת מקצה לקצה (6.9.2026):** זרימת
  עסקאות מדודה לכל זיפ, זיהום בנייה חדשה, 27 וריאציות ARV, ורשימת הכלים
  החסרים לפי עדיפות. `docs/experiments/` — הסקריפטים שמשחזרים כל טבלה
- `decisions.md` — למה זילו נפסל, למה לא בונים סורק, למה ARV קודם
- `tasks/todo.md` — תוכנית + תיקונים פתוחים
- `data/` — קבצי SDF וגיאוקוד, ~140MB. לא ב-git, נמחק בבטחה
