# דרך חדשה — מערכת החיתום

## פאזה 1 — מנוע ARV על נתונים ממשלתיים (חינם)

- [x] לאמת שלזילו אין API של מודעות — **אין. נסגר.**
- [x] לאמת שאין מוצר מדף שמריץ נוסחאות חיתום מותאמות — **13 מוצרים, אף אחד. נסגר.**
- [x] לאמת שאינדיאנה היא מדינת גילוי מלא ושהנתונים זמינים — **כן. IC 6-1.1-5.5.**
- [x] להוריד ולפרסר את קבצי ה-SDF 2023-2026
- [x] לחלץ מדיאן מחיר, DOM, אחוז קונים תופסים, אחוז מצוקה — לכל זיפ ב-BUY BOX
- [x] לזהות פליפים אמיתיים במכירה חוזרת על אותו parcel
- [x] לכמת הטיית שורדים (שיעור פגיעה 80%)
- [x] בדיקה עצמית ב-`sdf.py selftest`
- [x] **לחבר שטח בנוי** — נבדק לעומק. 🔴 **אין מקור חינמי בכמות.**
      שכבת החלקות (347,051, חינם) נראתה כפתרון אבל `ESTSQFT` שם הוא **שטח
      המגרש**. תיקיות Assessor/TaxAssessment ב-xmaps מוגבלות. כרטיס השומה
      מחזיר HTML לחלקה אחת בכל פעם. ראה `decisions.md`.
- [x] 🔲 **להחליט על מסלול ל-sqft** — **נסגר 6.9.2026 על סעיף (ג).**
      נבדק אמפירית ולא הוכרע בסברה: בקרה על גודל ואיכות (סינון קומפס לפי
      שווי שומה ±25%) משפרת את שגיאת חיזוי ה-ARV מ-13.6% ל-**12.6% בלבד**.
      ⇒ **מקור השגיאה הוא מצב הנכס ועומק השיפוץ, לא הגודל.**
      **(א) Regrid / Shovels.ai לא יקנו דיוק — נפסל.** (ב) מיותר.
      ראה `decisions.md`.
- [ ] למשוך מהשכבה החינמית את מה שכן יש: `ASSESSORYEAR_IMPTOTAL` (יחס
      מחיר/שומה = סיגנל תמחור-חסר) ו-`FULLOWNERNAME` (**מלאי חי של המתחרים**)
- [ ] להצליב 10-15 כתובות של Brooks ב-Accela מול 10-15 של Simple Quarters —
      אם רק ל-Brooks יש היתרים, זה מאשר את הפילוח **וחושף את שמות הקבלנים**
      ⚠️ 46236/46107/46224 אינם במערכת ההיתרים של אינדי (רשויות נפרדות)

## פאזה 1.6 — המסנן והמתחרים ✅

- [x] לענות למה כלל ה-70% תופס רק 19% — **האלגברה: מרווח נדרש = 0.30×מכירה + שיפוץ**
- [x] רגישות לתקציב שיפוץ ולערך הכלל (טבלה 5×4)
- [x] פילוח לפי מדרגת מחיר — **הממצא: 15% ב-$200K, 38% ב-$300-400K**
- [x] הפרדת wholesale מפליפ שיפוץ לפי חתימת החזקה
- [x] פרופיל 6 המתחרים מרישומי המכר: ערוץ רכישה, טייטל, החזקה, קונה קצה
- [x] מחקר רשת משלים: זהות תאגידית, שיווק, מודל מכירה
- [x] תיעוד מלא ב-`docs/מסנן_החיתום_ומתחרים.md` + 4 רשומות ב-`decisions.md`
- [x] 🔴 תיקון עצמי: `C8_Market_Days` לא שמיש — 91% אפסים = "לא דווח"

## פאזה 1.5 — מפת העסקאות ✅

- [x] גיאוקוד כל הכתובות — לשכת המפקד, חינם, **97% התאמה** (11,962/12,325)
- [x] גבולות 33 זיפים מטבלת המחקר (8 BUY BOX בולטים, WATCH/AVOID עמומים)
- [x] 1,215 פליפים על המפה, צבע וגודל לפי המרווח הגולמי
- [x] שכבת בסיס OSM — כבישים, תעשייה, מסחר ופארקים כבר מצוירים
- [x] 20 עוגני תעסוקה/מסחר מהמחקר הגאוגרפי + מסדרון ה-Blue Line
- [x] סינון: זיפ · מחיר · מרווח · **תקציב שכלל ה-70% מתיר** · חודשים · שנה · מצוקה · קונה תופס
- [x] 15 חברות טייטל לפי נפח + 20 קונים חוזרים (התחרות)
- [x] אומת בדפדפן — אין שגיאות קונסול, כל הסינונים מגיבים

## תיקונים לחוברת האקסל (נמצאו 6.9.2026)

- [x] 🔴 `תיק_500K!B7` היה `4` מתקופת המינוף — הוצג הון מושקע $966,920
      על תקציב $500K ו-`B26` שלילי. **תוקן ל-`=MIN(B6,2)`.**
- [ ] 🔴 `B31` מחזורים בשנה `=12/B30` מניח אפס זמן בין עסקאות.
      חסר חיפוש+סגירה של העסקה הבאה (~45 יום). 1.68 → ~1.39.
- [ ] 🟡 `B7` (ARV $/sqft) — להחליף את המכפיל 1.18 בנתון מדוד לכל זיפ
- [ ] 🟡 `B28` DOM=30 — מאומת נכון ל-2025-2026, אבל 46228 ו-46219 גבוהים מזה
- [ ] 🟢 לבדוק flat-fee MLS ($99-595) מול עמלת רישום 2.738% — שווה ~$7,900 לעסקה

## פאזה 1.7 — מנוע ARV בקומפס + טווח רווח ✅

**למה:** מכפיל ה-1.18 הוא ניחוש, ו-`decisions.md` (6.9.2026) מראה שאף מכפיל
ברמת זיפ לא מספיק מדויק. הכי טוב שנמדד בנתונים חינמיים הוא **קומפס
גאוגרפיים: 12.6% שגיאה חציונית, 42.9% ב-p90.** לכן המוצר אינו מספר ARV
בודד — הוא **טווח, והקומפס עצמם, לעין אנושית.**

קובץ חדש `arv.py`. שלוש עבודות, שלושה קבצים: `sdf.py` טוען את השוק ·
`analyze.py` חוקר את השוק · `arv.py` חותם נכס בודד.

- [x] `python3 arv.py comps <חלקה | "כתובת">` — 10 קומפס עם מרחק ותאריך,
      + טווח ARV. קומפס = קונה תופס · 12 ח' אחורה · הקרוב ביותר · שומה ±25%
- [x] `python3 arv.py deal <נכס> --offer X --reno Y` — **טווח הרווח** על
      ארבע נקודות ARV, כולל הצעת כלל ה-70% בכל אחת
- [x] `python3 arv.py backtest` — **מייצר מחדש את טבלת הדיוק.** רץ ~18 שניות,
      מחזיר 12.6%/42.9% ומאשר שהקבועים בקובץ תואמים
- [x] `python3 arv.py selftest` — עובר. הפ"ל מאומת מול עסקת הדוגמה ב-`CLAUDE.md`
- [x] לעדכן `STATUS.md` ו-`README.md`

**מה במכוון לא נעשה:** לא נגענו בקובץ האקסל בקוד (ספריית תקן בלבד;
עריכת xlsx ביד היא בדיוק הסוג של חוכמה שמתפוצצת). הטווח מחושב ב-Python.

### ✅ אימות על עסקה אמיתית — 6259 Chadworth Court, 46236

נקנתה ב-$241,469 ונמכרה ב-$324,000 (8.5.2026). הכלי, בלי לראות את המכירה:

| תרחיש | רווח |
|---|---|
| תחתון (−12.6%) | **−$683 ❌** *(היה −$2,710 לפני שעלויות הרכישה הפכו לקבועות, 6.9)* |
| **בפועל (ARV $324,000)** | **−$5,928** *(היה −$7,955)* |

**הכלי פסל עסקה שהפסידה כסף.** כלל ה-70% היה מתיר $174,384 — הקונה שילם
$67,085 מעל.
⚠️ ההפסד מחושב **על סקופ שיפוץ של $52,416.** אם הקונה עשה שיפוץ קל יותר
ייתכן שהרוויח. מה שהוכח: **בסקופ שלנו, במחיר הזה, זו עסקה מפסידה.**

### 🔲 נותר פתוח ב-`arv.py`
- [ ] **חיתום מודעה חיה** — כרגע `_resolve` מוצא רק נכסים שנמכרו ב-2023-26.
      נכס שלא נסחר בחלון לא ניתן לחיתום. צריך גיאוקוד של כתובת בודדת
      + שליפת שווי שומה מ-`xmaps` לפי חלקה. שתי הנקודות מאומתות וחינמיות
- [ ] **`ERR_P50`/`ERR_P90` הם קבועים מכוילים.** אחרי עדכון נתונים —
      להריץ `backtest`, והוא יגיד אם צריך לכייל מחדש

## פאזה 2 — סורק מודעות פעילות (נדחה)

נדחה במכוון. דורש מפתח API בתשלום, והדירוג יהיה חסר משמעות עד שמכפיל
ה-ARV מאומת. ברירת המחדל אם נחזור לזה: OpenWeb Ninja $25/ח', מאחורי מתאם מתחלף.

---

## Review

**פאזה 1 עובדת.** `sdf.py` מושך 4 שנים של נתוני מכר ממשלתיים ומחזיר
14,049 מכירות ו-1,159 פליפים מזוהים ב-8 זיפי ה-BUY BOX (עודכן 6.9: בלי בנייה חדשה; היה 1,246). בדיקה עצמית עוברת.

**מה שהתגלה ולא ידענו:**
- מכפיל הפליפ החציוני בפועל: **1.555** (קנייה→מכירה חוזרת), חציון החזקה 4.4 ח' *(עודכן 6.9; היה 1.58 / 4.6)*
- שיעור פגיעה 80% — 6% מהמכירות החוזרות הפסידו מעל 5%
- העסקה שבמודל יושבת ב**אחוזון 87** של הפליפים שבוצעו בפועל
- DOM עלה 20→30 בין 2023 ל-2025 ונשאר שם

**מה שעדיין חסר:** שטח בנוי. בלעדיו אי אפשר לחשב $/sqft, ולכן אי אפשר
עדיין להחליף את המכפיל 1.18 במספר מדוד. זה הפריט החוסם היחיד בפאזה 1.

**המפה (פאזה 1.5) עובדת ואומתה בדפדפן.** מה שהיא חשפה:
- **רק 16% מהפליפים היו עוברים את כלל ה-70% בתקציב שיפוץ $52K** *(עודכן 6.9 אחרי סינון בנייה חדשה; היה 19%)*.
  זה המסנן האמיתי, והוא הרבה יותר צר ממה שהמודל מרמז.
- ב-46236 Geist: 69 פליפים בסך הכל, **7** עם מרווח מעל $60K אחרי כלל ה-70%,
  ומהם **4** שנסגרו תוך 6 חודשים. זו זרימת העסקאות האמיתית בזיפ הזה, ל-4 שנים.
- שני מפעילים כמעט-טהורים: **Simple Quarters LLC** (57 רכישות, 54 פליפים)
  ו-**Brooks Holdings LLC** (28/27). הם התחרות הישירה — וגם מקור עסקאות אפשרי.

## פאזה 1.8 — המלאי החי של המתחרים ✅

`python3 analyze.py inventory` — **עובד. 412 חלקות, 123 מהן בזיפי ה-BUY BOX.**
השכבה נוספה גם למפה (צ'קבוקס "🔑 המלאי החי של המתחרים").

### ✅ מה שכן אומת — הנתונים קיימים וזמינים
שאילתת `FULLOWNERNAME` בשכבת החלקות עובדת. ספירות חיות, 6.9.2026:

| מפעיל | חלקות בבעלות **עכשיו** |
|---|---|
| GRISE HOME | **170** |
| SIMPLE QUARTERS | **111** |
| OWNEZ HOLDINGS | 60 |
| AMERICAN INTERNATIONAL HOME | 51 |
| BROOKS HOLDINGS | 18 |
| **POWER HOUSE HOLDINGS (HomeGo)** | **2** ← עסק של מהירות, לא של מלאי |
| "HOMEGO" כשם בעלים | 0 — הבעלות רשומה על השם המשפטי בלבד |

הרשומה מחזירה: `STATEPARCELNUMBER` · `ZIPCODE` · `PROPERTY_SUB_CLASS` (510/511)
· `ASSESSORYEAR_TOTALAV` · `ASSESSORYEAR_IMPTOTAL` · `OWNERADDRESS` · `ACREAGE`.

### ✅ הבאג תוקן — האבחון נשמר כדי שלא יחזור
`_fetch_owner()` שולח `resultOffset` + `f=geojson` + `returnGeometry=true`.
נבדקו ארבע קומבינציות מול השרת:

| שאילתה | תוצאה |
|---|---|
| `f=geojson` + `returnGeometry=true` | ❌ `Failed to execute query` |
| `f=geojson` + `returnGeometry=false` | ✅ 18 רשומות |
| כל שאילתה עם **`resultOffset`** | ❌ נכשלת |
| **`f=json` + `returnGeometry=true` + `outSR=4326`, בלי offset** | ✅ **18 רשומות** |

👉 **השרת הזה (MapServer ישן) לא תומך ב-`resultOffset`, ולא ב-geojson יחד
עם גאומטריה.** התיקון: להוריד את לולאת ה-pagination ולעבור ל-`f=json` +
`returnGeometry=true` + `outSR=4326`, ולקרוא `features[].attributes`
במקום `features[].properties`.
⚠️ בלי pagination יש תקרה של 1,000 רשומות לשאילתה. המקסימום כרגע 170,
אבל **חובה לזהות חיתוך** (`exceededTransferLimit`) ולא לחתוך בשקט.

### ✅ מה שנעשה
- [x] `_fetch_owner()` תוקן: בלי pagination, `f=json`, `attributes`.
      **כישלון שאילתה זורק חריגה** במקום להחזיר רשימה ריקה — זה היה הבאג
- [x] נרמול מספר חלקה: `49-06-26-126-076.000-101` → `490626126076000101`.
      בלי זה שום חלקה לא התחברה למכירה. יש בדיקה עצמית
- [x] שכבה במפה, צבע לכל מפעיל, חלקות ה-BUY BOX גדולות יותר
- [x] חציון החזקה **לפי מפעיל ולא מצרפי** — המצרף (32.4 ח') נשלט כולו
      ע"י המשכיר ולכן היה חסר משמעות

## פאזה 1.9 — ארבעת הכלים שאושרו (6.9.2026, מתוך `docs/ביקורת_מודל_וכלים.md`)

סדר לפי תלות: 4 → 3 → 2 → 1. כל שלב: קוד → `selftest` → הרצה אמיתית → פלט.

- [x] **4. יישור `arv.py` לאקסל** — `reno_budget(sqft, year)`; `BUY_CLOSE = $3,865` קבוע;
      `--sqft --year`. selftest: 1,400×$32×1.17 = $52,416 · 1,400/1960 = $65,520 · הצעה כפולה ≠ סגירה כפולה
- [x] **3. עדכניות** — `sdf.stamp()` בכל הרצה ("נתונים עד 2026-06-30 · הורד לפני N ימים", אזהרה ≥14);
      `sdf.py refresh` הורץ: 24 שניות, 14MB; `YEARS` מהלוח + `sdf.files()` מדלג על שנה שעוד לא פורסמה (404)
- [x] **2. סינון בנייה חדשה ב-`find_flips`** — `_new_build()`: class 500 / `A4_Improvement=N` /
      `B3_Vacant_Land` / `D1=Y`. **1,246 → 1,159 פליפים · מעבר 19% → 16% · מכפיל 1.582 → 1.555 ·
      backtest 12.4%/42.2% (n=539, הקבועים בסבילות).** המפה נבנתה מחדש (1,148 ממופים)
- [x] **1. `listings.py`** — Redfin CSV (+ RentCast, ⚠️ לא נבדק בלי מפתח). שווי שומה **לפי מספר בית
      + רחוב** (שאילתת נקודה החזירה את השכן — נפסלה). הורץ על CSV בפורמט Redfin שנבנה מ-22 מכירות
      אמיתיות: 21 נחתמו, 21/21 שומות נמצאו, כפולים/קונדו/בלי-קואורדינטות דולגו, `--comps` מדפיס קומפס
- [x] תיעוד: `decisions.md` (3 רשומות), `STATUS.md`, `README.md`, באנר במסנן, `CLAUDE.md`

### Review — פאזה 1.9
- **מה עובד:** 4 selftests · `listings.py` מקצה לקצה · `refresh` · המפה. ~120 שורות חדשות נטו.
- **מה לא אומת:** `--rentcast` — אין מפתח. הכותרות המדויקות של ה-CSV האמיתי של Redfin — אין
  התחברות. הקורא מחפש לפי תחילית ונכשל בקול עם רשימת הכותרות שמצא; אם ההורדה הראשונה
  נכשלת, זה תיקון של דקה.
- **מה שהתגלה תוך כדי:** `arv.py` משתמש בשומה מרשומת המכירה האחרונה, `listings.py` בשומה
  הנוכחית — לאותו בית (146 S 5th Ave: $189,700 מול $215,300) זה נותן קומפס שונים. הנוכחית
  נכונה למודעה. נרשם ב-README.
- **הבא בתור:** ראה פאזה 1.10 — אושר ובוצע.

## פאזה 1.10 — ההחלטות מחלק ג' (אושרו 6.9.2026 בערב)

- [x] **ב5 יומן חיתום** — `underwriting_log.csv` בשורש הריפו, כל הרצת `arv.py deal`, עמודת `outcome` ידנית
- [x] **ב6 מתחרים מהנתונים** — `analyze.top_flippers()`: 15 הקונים הגדולים של פליפים עוברים ב-24 ח'
      (Brooks 20, 100% תופס, 3.4 ח'; HPMC/Elite Indy = פרודקט $700K בצפון). שורת "אוף-מרקט" (C8=0) נמחקה
- [x] **ב7 איחוד טעינה** — `sdf.load()` נושא `sid/buyer/title/appr`; מטמון `contacts_{year}.json` (1.5MB/שנה,
      נבנה פעם אחת ~30 שניות); 4 לולאות נמחקו מ-`analyze`/`map`; `report` על 12 זיפים = 15 שניות
- [x] **כלל 72%** — `sdf.RULE`, אקסל B76/A76/A77, כל הטקסטים דינמיים. BUY BOX: 19% מעבר (היה 16%)
- [x] **שכבה צפונית** — `sdf.NORTH`, `sdf.TARGET`; מפה בצבע נפרד; WATCH הצטמצמה. `docs/בדיקה_זיפים_צפוניים.md`
- [x] **אקסל בקוד (openpyxl, חד-פעמי)** — B61 1.5%→1.9%, C61, B31 `=12/(B30+1.5)`, B76 72%, C7, הנחות B24/C24/B27/C27/A31,
      זיפים C14/C16/C19/C21 → "BUY BOX צפון", A43. `fullCalcOnLoad` — אקסל מחשב מחדש בפתיחה. המקור ב-git
- [x] backtest על 12 זיפים: מאוחד 13.6%/48.7% (n=824), אבל **לפי שכבה: BUY BOX 12.4%/42.9%, צפון
      18.7%/57.4%** — פער של 6 נקודות ⇒ `arv.ERR` הפך למילון לפי שכבה, הטווח המודפס נקבע לפי הזיפ
- [ ] 🔴 **פוש נכשל** — `github.com/vanmtomer-newway/New-Way` לא קיים או לא נגיש ל-`tomer-collab`.
      הקומיטים מקומיים. תומר: ליצור את הריפו / לתת גישה / לשנות remote

### Review — פאזה 1.10
- **מה השתנה בפרודקט:** 12 זיפי יעד במקום 8; הצעה גבוהה ב-2% מה-ARV; הזרימה העוברת ב-24 ח' עלתה מ-41 ל-189.
- **מה לא אומת:** הצפון ב-Redfin sold-CSV ($/sqft לפי רבעון) — דורש הורדה; שתי הצעות קבלן על בית משנות ה-50.
- **טעות שנמנעה:** כמעט נשארתי עם קונסטנטת שגיאה מאוחדת (13.6%) בהנחה שהפער בין השכבות הוא נקודה.
  המדידה: 6 נקודות (12.4% מול 18.7%). על יציאה של $535K זה $34K בתרחיש התחתון. תוקן לפני שנרשם.
