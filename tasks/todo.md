# דרך חדשה — מערכת החיתום

## פאזה 1 — מנוע ARV על נתונים ממשלתיים (חינם)

- [x] לאמת שלזילו אין API של מודעות — **אין. נסגר.**
- [x] לאמת שאין מוצר מדף שמריץ נוסחאות חיתום מותאמות — **13 מוצרים, אף אחד. נסגר.**
- [x] לאמת שאינדיאנה היא מדינת גילוי מלא ושהנתונים זמינים — **כן. IC 6-1.1-5.5.**
- [x] להוריד ולפרסר את קבצי ה-SDF 2023-2026
- [x] לחלץ מדיאן מחיר, DOM, אחוז קונים תופסים, אחוז מצוקה — לכל זיפ ב-BUY BOX
- [x] לזהות פליפים אמיתיים במכירה חוזרת על אותו parcel
- [x] לכמת הטיית שורדים (שיעור פגיעה 80%)
- [x] בדיקה עצמית ב-`sdf.py selftest`
- [x] **לחבר שטח בנוי** — נבדק לעומק. 🔴 **אין מקור חינמי בכמות.**
      שכבת החלקות (347,051, חינם) נראתה כפתרון אבל `ESTSQFT` שם הוא **שטח
      המגרש**. תיקיות Assessor/TaxAssessment ב-xmaps מוגבלות. כרטיס השומה
      מחזיר HTML לחלקה אחת בכל פעם. ראה `decisions.md`.
- [x] 🔲 **להחליט על מסלול ל-sqft** — **נסגר 6.9.2026 על סעיף (ג).**
      נבדק אמפירית ולא הוכרע בסברה: בקרה על גודל ואיכות (סינון קומפס לפי
      שווי שומה ±25%) משפרת את שגיאת חיזוי ה-ARV מ-13.6% ל-**12.6% בלבד**.
      ⇒ **מקור השגיאה הוא מצב הנכס ועומק השיפוץ, לא הגודל.**
      **(א) Regrid / Shovels.ai לא יקנו דיוק — נפסל.** (ב) מיותר.
      ראה `decisions.md`.
- [ ] למשוך מהשכבה החינמית את מה שכן יש: `ASSESSORYEAR_IMPTOTAL` (יחס
      מחיר/שומה = סיגנל תמחור-חסר) ו-`FULLOWNERNAME` (**מלאי חי של המתחרים**)
- [ ] להצליב 10-15 כתובות של Brooks ב-Accela מול 10-15 של Simple Quarters —
      אם רק ל-Brooks יש היתרים, זה מאשר את הפילוח **וחושף את שמות הקבלנים**
      ⚠️ 46236/46107/46224 אינם במערכת ההיתרים של אינדי (רשויות נפרדות)

## פאזה 1.6 — המסנן והמתחרים ✅

- [x] לענות למה כלל ה-70% תופס רק 19% — **האלגברה: מרווח נדרש = 0.30×מכירה + שיפוץ**
- [x] רגישות לתקציב שיפוץ ולערך הכלל (טבלה 5×4)
- [x] פילוח לפי מדרגת מחיר — **הממצא: 15% ב-$200K, 38% ב-$300-400K**
- [x] הפרדת wholesale מפליפ שיפוץ לפי חתימת החזקה
- [x] פרופיל 6 המתחרים מרישומי המכר: ערוץ רכישה, טייטל, החזקה, קונה קצה
- [x] מחקר רשת משלים: זהות תאגידית, שיווק, מודל מכירה
- [x] תיעוד מלא ב-`docs/מסנן_החיתום_ומתחרים.md` + 4 רשומות ב-`decisions.md`
- [x] 🔴 תיקון עצמי: `C8_Market_Days` לא שמיש — 91% אפסים = "לא דווח"

## פאזה 1.5 — מפת העסקאות ✅

- [x] גיאוקוד כל הכתובות — לשכת המפקד, חינם, **97% התאמה** (11,962/12,325)
- [x] גבולות 33 זיפים מטבלת המחקר (8 BUY BOX בולטים, WATCH/AVOID עמומים)
- [x] 1,215 פליפים על המפה, צבע וגודל לפי המרווח הגולמי
- [x] שכבת בסיס OSM — כבישים, תעשייה, מסחר ופארקים כבר מצוירים
- [x] 20 עוגני תעסוקה/מסחר מהמחקר הגאוגרפי + מסדרון ה-Blue Line
- [x] סינון: זיפ · מחיר · מרווח · **תקציב שכלל ה-70% מתיר** · חודשים · שנה · מצוקה · קונה תופס
- [x] 15 חברות טייטל לפי נפח + 20 קונים חוזרים (התחרות)
- [x] אומת בדפדפן — אין שגיאות קונסול, כל הסינונים מגיבים

## תיקונים לחוברת האקסל (נמצאו 6.9.2026)

- [x] 🔴 `תיק_500K!B7` היה `4` מתקופת המינוף — הוצג הון מושקע $966,920
      על תקציב $500K ו-`B26` שלילי. **תוקן ל-`=MIN(B6,2)`.**
- [ ] 🔴 `B31` מחזורים בשנה `=12/B30` מניח אפס זמן בין עסקאות.
      חסר חיפוש+סגירה של העסקה הבאה (~45 יום). 1.68 → ~1.39.
- [ ] 🟡 `B7` (ARV $/sqft) — להחליף את המכפיל 1.18 בנתון מדוד לכל זיפ
- [ ] 🟡 `B28` DOM=30 — מאומת נכון ל-2025-2026, אבל 46228 ו-46219 גבוהים מזה
- [ ] 🟢 לבדוק flat-fee MLS ($99-595) מול עמלת רישום 2.738% — שווה ~$7,900 לעסקה

## פאזה 1.7 — מנוע ARV בקומפס + טווח רווח ✅

**למה:** מכפיל ה-1.18 הוא ניחוש, ו-`decisions.md` (6.9.2026) מראה שאף מכפיל
ברמת זיפ לא מספיק מדויק. הכי טוב שנמדד בנתונים חינמיים הוא **קומפס
גאוגרפיים: 12.6% שגיאה חציונית, 42.9% ב-p90.** לכן המוצר אינו מספר ARV
בודד — הוא **טווח, והקומפס עצמם, לעין אנושית.**

קובץ חדש `arv.py`. שלוש עבודות, שלושה קבצים: `sdf.py` טוען את השוק ·
`analyze.py` חוקר את השוק · `arv.py` חותם נכס בודד.

- [x] `python3 arv.py comps <חלקה | "כתובת">` — 10 קומפס עם מרחק ותאריך,
      + טווח ARV. קומפס = קונה תופס · 12 ח' אחורה · הקרוב ביותר · שומה ±25%
- [x] `python3 arv.py deal <נכס> --offer X --reno Y` — **טווח הרווח** על
      ארבע נקודות ARV, כולל הצעת כלל ה-70% בכל אחת
- [x] `python3 arv.py backtest` — **מייצר מחדש את טבלת הדיוק.** רץ 37 שניות,
      מחזיר 12.6%/42.9% ומאשר שהקבועים בקובץ תואמים
- [x] `python3 arv.py selftest` — עובר. הפ"ל מאומת מול עסקת הדוגמה ב-`CLAUDE.md`
- [x] לעדכן `STATUS.md` ו-`README.md`

**מה במכוון לא נעשה:** לא נגענו בקובץ האקסל בקוד (ספריית תקן בלבד;
עריכת xlsx ביד היא בדיוק הסוג של חוכמה שמתפוצצת). הטווח מחושב ב-Python.

### ✅ אימות על עסקה אמיתית — 6259 Chadworth Court, 46236

נקנתה ב-$241,469 ונמכרה ב-$324,000 (8.5.2026). הכלי, בלי לראות את המכירה:

| תרחיש | רווח |
|---|---|
| תחתון (−12.6%) | **−$2,710 ❌** |
| **בפועל (ARV $324,000)** | **−$7,955** |

**הכלי פסל עסקה שהפסידה כסף.** כלל ה-70% היה מתיר $174,384 — הקונה שילם
$67,085 מעל.
⚠️ ההפסד מחושב **על סקופ שיפוץ של $52,416.** אם הקונה עשה שיפוץ קל יותר
ייתכן שהרוויח. מה שהוכח: **בסקופ שלנו, במחיר הזה, זו עסקה מפסידה.**

### 🔲 נותר פתוח ב-`arv.py`
- [ ] **חיתום מודעה חיה** — כרגע `_resolve` מוצא רק נכסים שנמכרו ב-2023-26.
      נכס שלא נסחר בחלון לא ניתן לחיתום. צריך גיאוקוד של כתובת בודדת
      + שליפת שווי שומה מ-`xmaps` לפי חלקה. שתי הנקודות מאומתות וחינמיות
- [ ] **`ERR_P50`/`ERR_P90` הם קבועים מכוילים.** אחרי עדכון נתונים —
      להריץ `backtest`, והוא יגיד אם צריך לכייל מחדש

## פאזה 2 — סורק מודעות פעילות (נדחה)

נדחה במכוון. דורש מפתח API בתשלום, והדירוג יהיה חסר משמעות עד שמכפיל
ה-ARV מאומת. ברירת המחדל אם נחזור לזה: OpenWeb Ninja $25/ח', מאחורי מתאם מתחלף.

---

## Review

**פאזה 1 עובדת.** `sdf.py` מושך 4 שנים של נתוני מכר ממשלתיים ומחזיר
14,049 מכירות ו-1,246 פליפים מזוהים ב-8 זיפי ה-BUY BOX. בדיקה עצמית עוברת.

**מה שהתגלה ולא ידענו:**
- מכפיל הפליפ החציוני בפועל: **1.58** (קנייה→מכירה חוזרת), חציון החזקה 4.6 ח'
- שיעור פגיעה 80% — 6% מהמכירות החוזרות הפסידו מעל 5%
- העסקה שבמודל יושבת ב**אחוזון 87** של הפליפים שבוצעו בפועל
- DOM עלה 20→30 בין 2023 ל-2025 ונשאר שם

**מה שעדיין חסר:** שטח בנוי. בלעדיו אי אפשר לחשב $/sqft, ולכן אי אפשר
עדיין להחליף את המכפיל 1.18 במספר מדוד. זה הפריט החוסם היחיד בפאזה 1.

**המפה (פאזה 1.5) עובדת ואומתה בדפדפן.** מה שהיא חשפה:
- **רק 19% מהפליפים (219 מ-1,171) היו עוברים את כלל ה-70% בתקציב שיפוץ $52K.**
  זה המסנן האמיתי, והוא הרבה יותר צר ממה שהמודל מרמז.
- ב-46236 Geist: 69 פליפים בסך הכל, **7** עם מרווח מעל $60K אחרי כלל ה-70%,
  ומהם **4** שנסגרו תוך 6 חודשים. זו זרימת העסקאות האמיתית בזיפ הזה, ל-4 שנים.
- שני מפעילים כמעט-טהורים: **Simple Quarters LLC** (57 רכישות, 54 פליפים)
  ו-**Brooks Holdings LLC** (28/27). הם התחרות הישירה — וגם מקור עסקאות אפשרי.
